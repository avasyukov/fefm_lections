#include <iostream>

// Это уже значительно чище, но ещё не всё.
// До конца фэн-шуй многофайловых проектов будет позже.
#include "utils.h"

int main() {
    int result = add(5, 7);
    std::cout << "Result: " << result << std::endl;
    return 0;
}